#!/usr/bin/env ruby

require_relative '../lib/timetable-parser'
require 'optparse'

options = {}
OptionParser.new do |opts|
  opts.banner = "Usage: timetable-parser"

  opts.on("-v", "--[no-]verbose", "Run verbosely") do |v|
    options[:verbose] = v
  end
end.parse!

p options
p ARGV
